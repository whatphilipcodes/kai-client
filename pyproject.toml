[project]
name = "kai-client"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = "~=3.14.0"
dependencies = [
    "kai-shared",
    "numpy>=2.5.2",
    "sounddevice>=0.5.5",
    "uvloop>=0.22.1",
]

[tool.uv.sources]
kai-shared = { path = "./kai-shared", editable = true }

[dependency-groups]
dev = [
    "ruff>=0.16.2",
    "ty>=0.0.70",
]
